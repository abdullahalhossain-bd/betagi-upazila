<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:orientation="vertical"
    android:gravity="bottom"
    android:background="@color/premium_primary"
    android:padding="20dp">

    <androidx.cardview.widget.CardView
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:cardElevation="0dp"
        app:cardCornerRadius="32dp"
        app:cardBackgroundColor="@color/premium_surface">
        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@mipmap/ic_launcher"
            android:scaleType="centerCrop"
            android:contentDescription="প্রোফাইল" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/header_short"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="বেতাগী ই-সেবা"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        android:fontFamily="@font/bnlatxf"
        android:layout_marginTop="12dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="সকল সেবা ও তথ্য এক প্ল্যাটফর্মে"
        android:textColor="#DDEDE7"
        android:textSize="12sp"
        android:fontFamily="@font/bnlatxf"
        android:layout_marginTop="2dp" />

</LinearLayout>
